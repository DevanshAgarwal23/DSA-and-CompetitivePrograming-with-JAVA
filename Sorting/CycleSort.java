package Sorting;

public class CycleSort {
    /*
    * Worst case O(N^2) sorting algo
    * Does minimum memory writes and can be useful for cases where memory writes are costly.(Memory like EEP ROM ,in this case cycle sot is useful)
    * In-place(means , doesn't take extra space) and Not-stable
    * Useful to solve question like find minimum swaps required to sort an array
    */



}
